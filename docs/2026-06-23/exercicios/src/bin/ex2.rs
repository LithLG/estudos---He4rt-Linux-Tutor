fn main() {
    let a = 10;
    let b = 5;

    let soma = a + b;
    let sub = a - b;
    let mult = a * b;
    let div = a / b;

    println!(
        "{a} + {b} = {soma}\n
{a} - {b} = {sub}\n
{a} * {b} = {mult}\n
{a} / {b} = {div}\n"
    );
}
